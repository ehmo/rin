# Infrastructure

Infrastructure-as-code and environment configuration.

- `terraform/`: IaC modules and stacks.
- `kubernetes/`: deployment manifests and overlays.
- `observability/`: metrics/logging/tracing setup.
- `environments/`: environment-specific config (`dev`, `staging`, `prod`).
